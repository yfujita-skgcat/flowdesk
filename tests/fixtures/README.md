# Test Fixtures

Use synthetic data for initial tests. Do not commit large FCS files.

`project_v0_1_channel_names.json` is a synthetic legacy manifest used to prove
that channel identity migration preserves references and unknown fields.
