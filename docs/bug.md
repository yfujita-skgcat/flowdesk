
# その他
- サンプルの順番を入れ替えられるようにする。ドラッグで移動できるようにしてほしい。また、キーボードの上、下でも移動できるようにしてほしい。
- add statistics でparametarが選べないことがある。
- close project を実装して、プロジェクトを閉じられるようにする
- File menu にあるopen directory とか open file とかはなに? 不要なら削除する。
